# 2026-03-01 Daily Notes

## crag 測試指令

```bash
# 導入 repo 到知識圖譜
crag rag $repo_path $repo_id --lightweight

# 測試 chat
crag chat $repo_id --question "$QUESTION_STR"

# 例如：
crag rag /home/ubuntu/workspace/open-gpu-kernel-modules nvidia-gpu --lightweight
crag chat nvidia --question "What is this project about?"
```

## crag 代碼修改

### README 注入功能
- 檔案: `/home/ubuntu/workspace/crag/codebase_rag/services/scheduler.py`
- 新增 `_load_readme_context()` 方法（第 88-140 行）
- 在第一次迭代時自動注入 README 內容到 prompt（第 209-213 行）
- **注意**: 需要用絕對路徑導入 repo，否則會找不到 README

### 全局異常處理
- 檔案: `/home/ubuntu/workspace/crag/codebase_rag/main.py`
- 新增 `_log_exception()` 函數，將未捕獲的異常寫入 log/debug.log

### Scheduler 終止條件修復
- 檔案: `/home/ubuntu/workspace/crag/codebase_rag/services/scheduler.py`
- 修改 `run_iteration()` 中的終止邏輯
- 如果 `is_text_only=True` 且回答看起來完整（>100字元，不是問句），直接接受為最終答案

### main.py 循環退出問題（已修復）
- 問題: `while True` 循環沒有正確退出，stdin 被關閉後嘗試讀取
- 修復:
  1. 添加 `should_exit` 標誌
  2. 添加 `EOFError` 異常處理
  3. 修復 `trace_ledger` 檢查 - 只有深度追蹤模式才生成 Final Flow Summary
  4. 簡單問答模式直接退出
- **狀態**: 已修復 ✅

## 問題排查

1. **repo 路徑問題**: 必須用絕對路徑導入，否則 chat 時找不到文件
2. **Embedding 端點**: 確保 `192.168.0.231:11434` 可連接
3. **tree_sitter_c**: 如果報錯，可能需要重新編譯

## NVIDIA GPU 驅動專案
- Repo: `/home/ubuntu/workspace/open-gpu-kernel-modules`
- 大小: 244 MB
- C 文件: 1,467 個
- Header 文件: 2,245 個

## 待辦事項
- [ ] 修復 main.py 循環退出問題（用 Gemini CLI）
- [ ] 測試完整流程
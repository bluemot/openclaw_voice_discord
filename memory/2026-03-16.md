# Memory Log - 2026-03-16

## AICOS Project Progress

### ✅ Completed

1. **Fixed dialogue history format bug**
   - Database format `{'speaker': 'User', 'message': '...'}` → LLM format `{'role': 'user', 'content': '...'}`
   - This was causing LLM API calls to fail with validation errors

2. **Fixed API Key loading**
   - Created `.env` file for environment variables
   - Modified `discord_bot.py` to load from `.env`
   - Added `.gitignore` to prevent committing sensitive data

3. **Fixed generator reuse issue**
   - Added `self.generator` and `self.current_character_id` to CosBot
   - Generator now persists across messages for same character
   - Emotion state now accumulates correctly

4. **Added Emotion Engine**
   - Created `core/emotion_engine.py` with dynamic emotion calculation
   - Based on: patience, temper, sensitivity (derived from MBTI + keywords)
   - 6 emotion levels: CALM → MILD → ANNOYED → IRRITATED → ANGRY → FURIOUS
   - Time decay (cooldown) mechanism
   - Content analysis for irritation factors

5. **Fixed local test flow**
   - Modified `--test`, `--test-trusted`, `--test-interactive` arguments
   - Now uses actual CosBot class with proper initialization
   - Mock Discord objects (MockMessage, MockChannel, MockUser)
   - Calls `bot.on_message()` like real Discord flow

6. **Updated system prompt with emotion states**
   - Added emotion context to Chinese system prompt
   - Different instructions for each emotion level
   - Dynamic length guidance based on emotion

### ⏳ TODO / Issues

1. **Emotion escalation not affecting response length**
   - Current: Responses stay short (2-5 characters) even when emotion escalates
   - Expected: Responses should get longer (10-20 characters) when irritated/angry
   - System prompt correctly shows emotion state and length guidance
   - Validator max_length updates correctly (5 → 8 → 15 → 20)
   - But LLM still generates short responses
   - **Need to investigate**: Why LLM ignores length instructions in system prompt

2. **Possible solutions to try:**
   - Increase temperature for more variation
   - Add explicit examples in system prompt
   - Use different wording ("must" instead of "can")
   - Consider post-processing to enforce length
   - Check if LLM model has specific length preferences

### 📁 Files Modified

- `bots/discord_bot.py` - Main bot, test functions
- `core/generator.py` - System prompt building, emotion integration
- `core/validator.py` - Dynamic max_length based on emotion
- `core/emotion_engine.py` - New emotion calculation system
- `.env` - Environment variables
- `.gitignore` - Ignore sensitive files
- `test_local_bot.py` - Local test using CosBot

### 🎯 Current Status

Bot is functional with:
- ✅ Working LLM API calls
- ✅ Character loading from database
- ✅ Emotion state tracking
- ✅ Generator reuse across messages
- ⚠️ Emotion escalation visible in logs but not in response length

Next step: Fix response length to reflect emotion escalation.

---
*Last updated: 2026-03-16 19:17*
